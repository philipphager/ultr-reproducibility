# @package _global_

defaults:
  - override /hydra/launcher: submitit_slurm

hydra:
  launcher:
    nodes: 1
    mem_gb: 32
    cpus_per_task: 8
    array_parallelism: "${num_jobs}"
    partition: gpu
    gres: gpu:a6000:1
